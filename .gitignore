# Blueprint build artifacts (generated by blueprint -build, not needed in repo)
# mysqlautobackup.blueprint is intentionally tracked — it's the release artifact

# Dev / editor
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Node (if any local tooling is added later)
node_modules/
